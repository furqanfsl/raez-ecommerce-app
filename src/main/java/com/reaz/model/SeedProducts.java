package com.reaz.model;

/**
 * SeedProducts is no longer used directly.
 * Seed data is now handled by ProductService.loadSeedData()
 * and DBConnection.seedSupportingData()
 *
 * This file is kept to avoid breaking any existing references.
 */
public class SeedProducts {
    // Seed data moved to ProductService.loadSeedData()
}